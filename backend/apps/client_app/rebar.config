{deps, [jsx, cowboy]}.

{shell,
 % {config, "config/sys.config"},
 [{apps, [client_app]}]}.

{relx,
 [{release, {client_app, "1.0"}, [client_app]},
  {mode, prod},
  {sys_config, "./config/sys.config"}]}.
