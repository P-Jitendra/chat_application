{deps, [ranch, gproc, jsx, iso8601, cowboy]}.

{shell,
 % {config, "config/sys.config"},
 [{apps, [server_app]}]}.

{relx,
 [{release, {server_app, "1.0"}, [server_app]},
  {mode, prod},
  {sys_config, "./config/sys.config"}]}.
